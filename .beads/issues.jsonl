{"id":"effect-qb-dn3.1","title":"P0 migrate Effect dependencies and schema API surface","description":"Smallest coherent implementation slice. Align workspace/package dependencies to effect@4.0.0-beta.66 and matching v4 packages, remove @effect/experimental where replaced, and perform the mechanical Schema API migration called out in docs/effect-v4-opportunities.md without intentional behavior changes.","acceptance_criteria":"Effect package versions are aligned to the observed v4 beta line; v3 Schema call shapes listed in the opportunity doc are migrated; README/type-test examples are updated as needed; behavior tests covering risky runtime schema/json paths still pass; bun run test:types, bun test, and relevant tsgo checks are run or blockers documented.","notes":"Status: in_progress. Starting with repo inspection and focused regression/type coverage around current Schema API and JSON path inference before the mechanical v4 dependency/API migration.\nBaseline before migration: focused Bun behavior tests passed for runtime-value/json/table/executor; tsgo type-test project passed. Commands: bun --config ./bunfig.test.toml test test/internal/behavior/runtime-value.behavior.test.ts test/public/behavior/json.behavior.test.ts test/internal/behavior/table.test.ts test/public/behavior/executor.behavior.test.ts; bunx tsgo -p tsconfig.type-tests.json.\nImplemented and verified in the first migration slice. Dependencies aligned to effect@4.0.0-beta.66 and matching v4 driver/platform packages; removed @effect/experimental, @effect/sql, @effect/cli, and @effect/platform where v4 unstable exports replaced them; migrated mechanical Schema API changes and README/type examples. Checks passed: bunx tsgo -p tsconfig.type-tests.json; bun --config ./bunfig.test.toml test test/internal/behavior/runtime-value.behavior.test.ts test/public/behavior/json.behavior.test.ts test/internal/behavior/table.test.ts test/public/behavior/executor.behavior.test.ts test/public/behavior/mysql-errors.behavior.test.ts test/public/behavior/postgres-errors.behavior.test.ts; bun run test:types; bun run build; bun run test:pack; bun run test.","status":"closed","priority":0,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:18Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:41:33Z","started_at":"2026-05-13T02:20:14Z","closed_at":"2026-05-13T02:41:33Z","close_reason":"P0 dependency and mechanical Schema API migration implemented; full type, build, pack, focused behavior, and full Bun tests passed.","labels":["effect-v4","migration","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.1","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:18Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":0,"dependent_count":6,"comment_count":0}
{"id":"effect-qb-dn3","title":"Effect v4 migration epic","description":"Parent epic for the Effect v4 migration work described in docs/effect-v4-opportunities.md. Scope includes dependency alignment, mechanical Schema API migration, VariantSchema replacement, v4 SQL core adoption, SqlSchema validation, optional v4 codecs, row decode error improvements, JSON schema inference review, and CLI/platform packaging.","acceptance_criteria":"All child beads map to docs/effect-v4-opportunities.md opportunities; every opportunity is implemented, explicitly deferred, or blocked with a concrete reason; relevant tests/type checks/lint checks pass; coherent increments are committed.","notes":"Source of truth: docs/effect-v4-opportunities.md checked against effect@4.0.0-beta.66. Assumption: this branch is the v4 line, so no Effect 3 compatibility shim is required unless a child bead discovers a concrete compatibility requirement.","status":"open","priority":0,"issue_type":"epic","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:02Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:18:02Z","labels":["effect-v4","migration"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-992","title":"Audit querybuilder correctness bugs","description":"Audit effect-qb for concrete correctness bugs. A bug only counts after a red spec-style test reproduces it; then patch implementation and rerun focused/broad gates.","acceptance_criteria":"At least one audited slice either lands a red-green bug fix with tests and gates, or documents no confirmed bug after concrete source/test inspection; committed and pushed before session close.","notes":"Landed six red-green fixes in this audit slice: cased FK DDL targets, MySQL/SQLite query casing, MySQL/SQLite DDL casing, Postgres DDL name casing, Postgres schema-model physical casing, and mutation insert/conflict casing. Verified with bun run build, bun test test/public/behavior test/internal/behavior (540 pass), and bunx tsgo -p tsconfig.type-tests.json --pretty false.\nContinued audit with two additional red-green fixes: generated column DDL expressions now use table-level casing, and MySQL joined delete target lists now use cased physical table identifiers. Verified with bun run build, bun test test/public/behavior test/internal/behavior (542 pass), and bunx tsgo -p tsconfig.type-tests.json --pretty false.\nLanded another red-green audit slice: MySQL and SQLite update assignments now use the mutation target table when applying table-level casing, so cased models render physical assignment identifiers. Red spec first failed with MySQL rendering displayName instead of display_name. Verified with focused casing spec, affected MySQL/SQLite behavior suites, bun run build, full behavior suite (543 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: renderer-level casing now preserves query-local source aliases and columns for values/derived-style sources instead of casing references to names the source does not export. Red spec first failed with Postgres rendering seed_rows.display_name while the values source declared SeedRows(displayName). Verified with focused casing spec, casing plus select-source behavior suites, bun run build, full behavior suite (544 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: MySQL and SQLite nested renderers now carry renderer casing and source-name mappings into derived/subquery render state. Red spec first failed with a cased renderer rendering the outer derived reference as ActiveUsers.displayName but the MySQL nested subquery still used raw UserAccounts.displayName. Verified with focused derived-source casing spec, affected casing/source/MySQL/SQLite behavior suites, bun run build, full behavior suite (545 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed between predicate ASTs now reject with a clear operand-count error across Standard, Postgres, MySQL, and SQLite renderers instead of dereferencing undefined. Red spec first failed with undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (546 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed coalesce expression ASTs now reject with a clear value-count error across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid coalesce() SQL. Red spec first failed with Standard rendering select coalesce() as email. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (547 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed concat expression ASTs now reject with a clear arity error across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid empty concatenation SQL. Red spec first failed with Standard rendering select () as label. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (548 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed boolean combinator ASTs now reject with clear and/or predicate-count errors across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on ast.values.length. Red spec first failed with Standard throwing undefined is not an object while evaluating ast.values.length. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (549 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed membership predicate ASTs now reject with clear in/notIn candidate-count errors across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on ast.values.length. Red spec first failed with Standard throwing undefined is not an object while evaluating ast.values.length. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (550 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed case expression ASTs now reject with clear branch-count errors across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid case-else SQL. Red spec first failed with Standard rendering select case else as label. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (551 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed case expression ASTs now reject missing fallback expressions with a clear else-expression error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on expression metadata. Red spec first failed with Standard throwing undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (552 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed case expression ASTs now reject incomplete branch entries with a clear when/then error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on expression metadata. Red spec first failed with Standard throwing undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (553 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed window over expression ASTs now reject missing aggregate values with a clear error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on expression metadata. Red spec first failed with Standard throwing undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (554 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed window order ASTs now reject invalid order directions with the existing asc/desc error across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid SQL. Red spec first failed with Standard rendering row_number() over (... sideways). Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (555 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed window order ASTs now reject missing order expression values with a clear error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on expression metadata. Red spec first failed with Standard throwing undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (556 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed window partition ASTs now reject missing partition expression values with a clear error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing on expression metadata. Red spec first failed with Standard throwing undefined is not an object while evaluating expression[ExpressionAst.TypeId]. Verified focused rendering spec, Postgres/MySQL/SQLite renderer suites, bun run build, full behavior suite (557 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed quantified comparison ASTs now reject invalid comparison operators with a clear error across Standard, Postgres, MySQL, and SQLite renderers instead of silently rendering them as \u003e=. Red spec first failed with Standard rendering \"sideways\" as \u003e= any. Verified focused rendering/select-source/sqlite/Postgres/MySQL renderer suites, bun run build, full behavior suite (558 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed collate expression ASTs now reject missing collation identifiers before rendering SQL instead of emitting invalid collate clauses. Red spec first failed with Standard rendering \"select (? collate ) as \\\"email\\\"\". Verified focused rendering/Postgres/table suites, bun run build, full behavior suite (559 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed current_date function ASTs now reject unexpected arguments across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid current_date(?) SQL. Red spec first failed with Standard rendering \"select current_date(?) as \\\"today\\\"\". Verified focused rendering/Postgres/MySQL/SQLite/function suites, bun run build, full behavior suite (560 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: extract function calls now reject wrong arity across Standard, Postgres, MySQL, and SQLite renderers instead of falling through to invalid extract(...) function-call SQL. Red spec first failed with Standard rendering \"select extract(?) as \\\"extracted\\\"\". Verified focused rendering/Postgres/MySQL/SQLite/function suites, bun run build, full behavior suite (561 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: function calls now reject empty function names across Standard, Postgres, MySQL, and SQLite renderers instead of emitting invalid anonymous function-call SQL. Red spec first failed with Standard rendering \"select (?) as \\\"value\\\"\". Verified focused rendering/Postgres/MySQL/SQLite/function suites, bun run build, full behavior suite (562 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed function-call ASTs now reject missing argument arrays across Standard, Postgres, MySQL, and SQLite renderers instead of silently rendering invalid empty-argument calls. Red spec first failed with Standard rendering \"select lower() as \\\"value\\\"\". Verified focused rendering/Postgres/MySQL/SQLite/function suites, bun run build, full behavior suite (563 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed case expression ASTs now reach the renderer guard when the branch array is missing, instead of crashing in runtime dialect discovery. Red spec first failed with \"undefined is not an object (evaluating 'ast.branches.reduce')\". Verified focused rendering/source/dialect/table suites, bun run build, full behavior suite (564 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.\nLanded another red-green audit slice: malformed window expression ASTs now reject missing partition/order arrays with a clear error across Standard, Postgres, MySQL, and SQLite renderers instead of crashing in runtime dialect discovery. Red spec first failed with \"undefined is not an object (evaluating 'ast.orderBy.reduce')\". Verified focused rendering/Postgres/MySQL/SQLite/source/table suites, bun run build, full behavior suite (565 pass), bunx tsgo -p tsconfig.type-tests.json --pretty false, and git diff --check.","status":"open","priority":1,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-24T05:42:01Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T08:48:26Z","labels":["audit","querybuilder"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-cne","title":"Unify standard root API, casing, and Postgres schema composition","description":"Implement the proposed effect-qb API cleanup: root exports standard Column/Table/Query/etc directly without Sql, add Casing factories and pipeable metadata, move portable helpers to root standard modules, make dialect packages extension-only where practical, add composable Pg.Schema namespace, and tighten renderer valueMappings keys.","acceptance_criteria":"Root effect-qb exposes Column/Table/Query/Renderer/Executor/Casing without Sql; casing works at renderer/factory/schema/table levels with pipeable overrides; Pg.Schema.make supports table/enum/sequence and withSchema; portable primary/table/column helpers are available from root; valueMappings reject unknown keys per dialect; focused type and behavior tests pass; build and pack gates pass.","status":"closed","priority":1,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-24T04:29:51Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T05:39:03Z","started_at":"2026-05-24T04:30:07Z","closed_at":"2026-05-24T05:39:03Z","close_reason":"Implemented root standard API, casing, Pg.Schema composition, extension-only dialect surfaces, typed value mappings, and passing gates","labels":["api","dialect","querybuilder"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-yll.2","title":"Phase 2 — Introduce the standard dialect tag and MergeDialect type rule","description":"# Phase 2 — Introduce `\"standard\"` as a dialect tag\n\nDepends on Phase 1 (shared renderer must exist before a `\"standard\"` dialect can render).\n\nAdd the type-level machinery so plans can be tagged `\"standard\"` and renderable by every engine. Add the runtime `standardDialect` value that uses the shared renderer.\n\n## Substeps\n\n1. **Type-level merge rule first, with tests.** Add `MergeDialect\u003cA, B\u003e` in `src/internal/query.ts`:\n    ```ts\n    type ConcreteDialect\u003cD\u003e = D extends \"standard\" ? never : D\n    type DialectConflictError\u003cA extends string, B extends string\u003e = {\n      readonly _tag: \"DialectConflict\"\n      readonly left: A\n      readonly right: B\n    }\n    export type MergeDialect\u003cA extends string, B extends string\u003e =\n      [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [never, never] ? \"standard\"\n      : [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [never, infer C extends string] ? C\n      : [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [infer C extends string, never] ? C\n      : ConcreteDialect\u003cA\u003e extends ConcreteDialect\u003cB\u003e\n        ? ConcreteDialect\u003cB\u003e extends ConcreteDialect\u003cA\u003e ? A\n          : DialectConflictError\u003cA, B\u003e\n        : DialectConflictError\u003cA, B\u003e\n    ```\n    Write type-level tests asserting: `MergeDialect\u003c\"standard\",\"standard\"\u003e = \"standard\"`; `MergeDialect\u003c\"standard\",\"postgres\"\u003e = \"postgres\"`; `MergeDialect\u003c\"postgres\",\"standard\"\u003e = \"postgres\"`; `MergeDialect\u003c\"postgres\",\"postgres\"\u003e = \"postgres\"`; `MergeDialect\u003c\"postgres\",\"mysql\"\u003e = DialectConflictError\u003c...\u003e`.\n2. **Update existing combining helpers** in `src/internal/query.ts` (`TupleDialect` at line 1466, `ExtractDialect` at line 468) and `src/internal/query.d.ts` (`SourceDialectOf`, `PlanDialectOf` at line 496) to use `MergeDialect` instead of plain union.\n3. **Update `DialectCompatiblePlan`** in `src/internal/query.d.ts:496` so a plan whose dialect tag is `\"standard\"` (or a tag matching the engine name) is accepted by every renderer.\n4. **Create `src/standard/dialect.ts`** exporting `standardDialect: SqlDialect\u003c\"standard\"\u003e`. Implement every `SqlDialect` hook with portable SQL:1992-ish defaults: `\"`-quoted identifiers, `?` placeholders, `||` concat, `'string'` DDL strings, `true`/`false` booleans. Dialect-only features (`renderInsertConflict`, `renderReturning`, JSON mutations, etc.) throw `\"\u003cfeature\u003e not supported in standard SQL\"`.\n5. **Create `src/standard/renderer.ts`** that calls the shared `renderQueryAst` with `standardDialect` and exposes a `Renderer\u003c\"standard\"\u003e` (mirroring `src/postgres/renderer.ts`).\n6. **Tests**: build a hand-crafted `\"standard\"`-tagged plan; assert it renders successfully via `standard.Renderer`, `postgres.Renderer`, `mysql.Renderer`, and `sqlite.Renderer`, and the SQL is byte-identical to whatever the engine already produces for the equivalent native plan.\n\n## Files touched\n\n**Modified:** `src/internal/query.ts`; `src/internal/query.d.ts`.\n\n**Created:** `src/standard/dialect.ts`; `src/standard/renderer.ts`; type-test file for `MergeDialect`; cross-engine standard-render test.\n\n## Acceptance\n\n- `MergeDialect\u003cA, B\u003e` defined and type-tested; used by `TupleDialect`, `ExtractDialect`, `SourceDialectOf`, `PlanDialectOf`.\n- `DialectCompatiblePlan` accepts `\"standard\"` plans for every engine.\n- `standardDialect: SqlDialect\u003c\"standard\"\u003e` exists at `src/standard/dialect.ts`.\n- `src/standard/renderer.ts` exposes a working `Renderer\u003c\"standard\"\u003e`.\n- A `\"standard\"`-tagged plan renders identically on all three engines.\n- All pre-existing tests still pass.\n\n## Notes\n\n- `MergeDialect` is load-bearing. Type-level tests must land in step 1; everything downstream depends on its correctness.\n- No public surface change yet — `src/standard.ts` barrel is deferred to Phase 4.\n","acceptance_criteria":"MergeDialect\u003cA,B\u003e defined and type-tested (standard∪standard=standard; standard∪X=X; X∪X=X; postgres∪mysql=DialectConflictError); TupleDialect/ExtractDialect/SourceDialectOf/PlanDialectOf use it; DialectCompatiblePlan accepts standard plans on every engine; standardDialect: SqlDialect\u003c\"standard\"\u003e exists at src/standard/dialect.ts; src/standard/renderer.ts exposes a working Renderer\u003c\"standard\"\u003e; a hand-crafted standard plan renders identically on all three engines.","notes":"Partial pieces are present: MergeDialect and DialectConflictError are defined/type-tested, DialectCompatiblePlan accepts standard via the compatibility check, standardDialect and Standard.Renderer exist, and a cross-render behavior test exists. Not acceptance-complete: TupleDialect/ExtractDialect/SourceDialectOf/PlanDialectOf still use the existing union model because normalizing in those hot recursive helpers caused tsgo performance regressions; standardDialect only implements the current SqlDialect surface, not the expanded hook surface from Phase 1.\n2026-05-19 update: standard dialect typing advanced. NormalizeDialect now collapses standard unions in ExtractDialect, SourceDialectOf, PlanDialectOf, and TupleDialect; DialectCompatiblePlan/DialectCompatibleNestedPlan now check normalized PlanDialectOf. Added type coverage for pure standard plans rendering on postgres/mysql/sqlite and standard+postgres narrowing rejected by mysql renderer. Gates passed: bun test, bun run test:types, bun run build, bun run test:pack, bun run test:integration, git diff --check. Still open: helpers use NormalizeDialect rather than a full pairwise MergeDialect conflict reducer, and the expanded hook-only SqlDialect surface remains Phase 1 work.\n2026-05-23 update: standard dialect tag behavior is now covered by type tests for standard-only plans, Postgres/MySQL/SQLite narrowing, mixed concrete DialectConflictError, renderer compatibility, and executor compatibility. Renderer/executor runtime guards now reject untyped mixed concrete standard plans.","status":"open","priority":1,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:53:03Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T01:48:42Z","labels":["architecture","dialect","phase-2","querybuilder","refactor","types"],"dependencies":[{"issue_id":"effect-qb-yll.2","depends_on_id":"effect-qb-yll","type":"parent-child","created_at":"2026-05-19T13:53:02Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-yll.2","depends_on_id":"effect-qb-yll.1","type":"blocks","created_at":"2026-05-19T13:53:28Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"effect-qb-yll.1","title":"Phase 1 — Consolidate the three sql-expression-renderer.ts files into one shared renderer","description":"# Phase 1 — Renderer consolidation\n\nEliminate the duplication across `mysql/internal/sql-expression-renderer.ts` (1857 lines), `postgres/internal/sql-expression-renderer.ts` (1824 lines), and `sqlite/internal/sql-expression-renderer.ts` (1821 lines). Pairwise diffs are 720–1062 lines — they already share ~70–80%.\n\nPure internal refactor — no public API change.\n\n## Substeps\n\n1. **Add golden SQL snapshot test first** — before changing anything. Pick ~50 representative queries covering each branched-on feature: `ON CONFLICT`, `ON DUPLICATE KEY UPDATE`, `UPDATE...ORDER BY/LIMIT` (mysql), `RETURNING` (postgres/sqlite), `CREATE INDEX [IF NOT EXISTS]`, schema-qualified `DROP INDEX` (postgres), `CAST` to dialect-specific type names, DDL bytes (`x'...'` vs `decode(..., 'hex')`), DDL booleans (`true`/`false` vs `1`/`0`), JSON extract (`-\u003e`, `-\u003e\u003e`, `json_extract`, MySQL `$.path` syntax), JSON mutations (`jsonb_set` vs `json_set`/`json_replace`/`json_insert`). Snapshot the SQL emitted by each of the three engines today.\n2. **Expand `SqlDialect`** in `src/internal/dialect.ts` (currently 4 methods at line 37) with the new hooks: `renderDdlString`, `renderDdlBytes`, `renderDdlLiteral`, `renderCreateIndex`, `renderDropIndex`, `renderInsertConflict`, `renderReturning`, `renderUpdateOrderLimit`, `renderCast`, `renderBooleanLiteral`, `renderJsonExtract`, `renderJsonContains`, `renderJsonPathLiteral`, `renderJsonMutation`. Each new method takes a `base` reference to the shared expression renderer so dialects can recurse for sub-expressions.\n3. **Implement the new hooks** on `mysqlDialect` (`src/mysql/internal/dialect.ts`), `postgresDialect` (`src/postgres/internal/dialect.ts`), `sqliteDialect` (`src/sqlite/internal/dialect.ts`). Copy logic verbatim from the corresponding `internal/sql-expression-renderer.ts` files. For features a dialect doesn't support, throw a clear error (e.g. mysql `renderReturning` throws `\"Unsupported mysql returning\"`).\n4. **Create `src/internal/sql-expression-renderer.ts`** as the unified renderer. Start by copying `postgres/internal/sql-expression-renderer.ts` (median size). Replace every `if (dialect.name === \"...\")` branch and inline DDL/booleans/JSON/conflict/returning logic with a call to the corresponding `dialect.renderXxx(...)` method.\n5. **Rewrite `src/{mysql,postgres,sqlite}/internal/renderer.ts`** as thin (~30-line) wrappers that import `renderQueryAst` from `src/internal/sql-expression-renderer.ts` and pass their dialect record in.\n6. **Delete** `src/{mysql,postgres,sqlite}/internal/sql-expression-renderer.ts`.\n7. **Run** golden SQL tests, `bun test`, `bunx tsgo -p tsconfig.type-tests.json`, and `docker-compose.integration.yml`-driven integration tests. Every byte of emitted SQL must be unchanged.\n\n## Files touched\n\n**Modified:** `src/internal/dialect.ts`; `src/{mysql,postgres,sqlite}/internal/dialect.ts`; `src/{mysql,postgres,sqlite}/internal/renderer.ts`.\n\n**Created:** `src/internal/sql-expression-renderer.ts`; golden snapshot test file.\n\n**Deleted:** `src/{mysql,postgres,sqlite}/internal/sql-expression-renderer.ts`.\n\n## Acceptance\n\n- The three per-dialect `sql-expression-renderer.ts` files are gone.\n- One shared `src/internal/sql-expression-renderer.ts` is parameterized only on `SqlDialect` and contains no `dialect.name === \"...\"` branches.\n- Golden SQL snapshot tests pass with byte-identical output.\n- Full unit/type/integration test suites pass.\n- No change to any public export.\n","acceptance_criteria":"Per-dialect sql-expression-renderer.ts files deleted; one shared src/internal/sql-expression-renderer.ts produces byte-identical SQL on all three engines; golden snapshot test of ~50 representative queries passes; unit/type/integration suites all green; no public export changes.","notes":"Partial implementation landed: old per-dialect sql-expression-renderer.ts files were removed from mysql/postgres/sqlite paths and wrappers now call src/internal/sql-expression-renderer.ts. Current shared file dispatches to internal dialect-renderers, so the phase is not acceptance-complete: SqlDialect hooks are not expanded, src/internal/sql-expression-renderer.ts is not hook-only/no-branch, and the ~50-query golden snapshot has not been added. Local gates passed: bun test, bun run test:types, bun run build, bun run test:pack, bun run test:integration, git diff --check.\n2026-05-19 update: fixed stale internal tests to import the shared sql-expression-renderer path after old per-dialect paths were removed. Tried routing all dialects through the SQLite renderer copy to test whether one existing copy already covered the matrix; targeted behavior tests failed on Postgres/MySQL-specific rendering, so the dispatcher remains necessary until the real hook extraction lands. Gates passed after reverting the probe: bun test, bun run test:types, bun run build, bun run test:pack, bun run test:integration, git diff --check.\n2026-05-23 update: shared sql-expression-renderer now delegates query/expression rendering through the selected SqlDialect object, removing the central dialect-name dispatcher. Remaining phase-1 refactor work, if still desired, is the larger hook extraction/golden snapshot cleanup rather than the standard dialect compatibility boundary.","status":"in_progress","priority":1,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:52:55Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T01:48:21Z","started_at":"2026-05-19T20:59:37Z","labels":["architecture","dialect","phase-1","querybuilder","refactor","renderer"],"dependencies":[{"issue_id":"effect-qb-yll.1","depends_on_id":"effect-qb-yll","type":"parent-child","created_at":"2026-05-19T13:52:54Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"effect-qb-yll","title":"Extract standard SQL dialect that mysql/postgres/sqlite extend","description":"# Refactor: extract `standard` SQL dialect that mysql/postgres/sqlite extend\n\n## Context\n\nThe querybuilder package implements three SQL dialects (mysql, postgres, sqlite) as parallel sibling folders under `packages/querybuilder/src/`. The renderer pipeline is the worst offender for duplication:\n\n- `mysql/internal/sql-expression-renderer.ts` — 1857 lines\n- `postgres/internal/sql-expression-renderer.ts` — 1824 lines\n- `sqlite/internal/sql-expression-renderer.ts` — 1821 lines\n\nPairwise diffs are 720–1062 lines, meaning the three files share ~70–80% of their logic. Some files even contain `if (dialect.name === \"postgres\")` branches inside the *mysql* folder (e.g. `mysql/internal/sql-expression-renderer.ts:215`) — evidence the abstraction is straining. The `internal/dsl.ts` files (~6900 lines each) have similar duplication.\n\nThe type-level machinery for the target architecture **already exists**:\n- `SqlDialect\u003cName\u003e` interface at `src/internal/dialect.ts:37` with four hooks (`quoteIdentifier`, `renderLiteral`, `renderTableReference`, `renderConcat`).\n- A shared, dialect-neutral AST under `src/internal/` (query, query-ast, expression-ast, predicate, coercion, json, runtime).\n- Dialect-phantom-typed plans: `PlanDialectOf\u003cPlan\u003e`, `DialectOf\u003cValue\u003e`, `ExtractDialect\u003cSelection\u003e`, `TupleDialect\u003c...\u003e`, `SourceDialectOf\u003cSource\u003e` (`src/internal/query.ts:144,468,1466`).\n- `DialectCompatiblePlan` and `DialectCompatibilityError` already gate renderer compatibility (`src/internal/query.d.ts:496,662`).\n- `column`, `table`, `query` are already ~95% shared via `internal/` base modules; per-dialect wrappers exist mainly to apply a dialect phantom.\n\nWhat's missing is (a) a `\"standard\"` dialect tag that flows portably through the type system, (b) one unified renderer parameterized on `SqlDialect` instead of three copies, and (c) a `standard/` namespace users can import to write portable queries.\n\n## Target architecture\n\n### User-facing model\n\n```ts\nimport * as Std from \"@effect-qb/querybuilder/standard\"\nimport * as Pg from \"@effect-qb/querybuilder/postgres\"\n\n// Portable — typed \"standard\". Renders on any engine.\nconst q1 = Std.Query.update(users).set({ active: false })\n\n// Specialized — pulling in any Pg-only piece narrows the plan to \"postgres\".\nconst q2 = Std.Query.update(users)\n  .set({ active: false })\n  .from(Pg.Query.from(audit))           // ← postgres-only UPDATE...FROM\n  .returning((u) =\u003e [u.id])             // ← postgres/sqlite, not mysql\n```\n\nImporting a piece from `Pg` narrows the plan's dialect to `\"postgres\"`. Mixing a `Pg` piece with an `My` piece is a compile error.\n\n### Dialect tag rule\n\n- `\"standard\"` is a bottom element: portable, compatible with any engine.\n- `\"standard\" ∪ \"X\" = \"X\"` for any concrete `X` (the standard piece accepts any specialization).\n- `\"X\" ∪ \"X\" = \"X\"`.\n- `\"postgres\" ∪ \"mysql\" = DialectConflictError\u003c...\u003e` (compile-time error type, surfaced where it occurs).\n\nThe existing `TupleDialect` and `ExtractDialect` types are updated to use a new `MergeDialect\u003cA, B\u003e` helper that implements this rule. `DialectCompatiblePlan` is updated so a `\"standard\"`-tagged plan is renderable by every engine.\n\n### Runtime model\n\nType-only specialization. All dialect-tagged pieces — standard or specific — build the **same** universal AST. The renderer is the only place dialect-specific text is emitted; it does so by consulting an expanded `SqlDialect` record passed in by the engine. AST nodes do *not* carry render closures.\n\n## Folder layout after the refactor\n\n```\npackages/querybuilder/src/\n├── internal/                       (existing — minor additions)\n│   ├── dialect.ts                  ← SqlDialect interface, expanded with new hooks\n│   ├── sql-expression-renderer.ts  ← NEW: single shared renderer (was 3× ~1820 lines)\n│   ├── renderer.ts                 (unchanged — Renderer\u003cDialect\u003e factory)\n│   ├── query.ts                    (MergeDialect helper added; existing types updated)\n│   └── ...                         (everything else unchanged)\n├── standard/                       ← NEW public namespace\n│   ├── dialect.ts                  ← standardDialect: SqlDialect\u003c\"standard\"\u003e (defaults)\n│   ├── query.ts                    ← standard DSL: select/from/where/insert/update/delete/...\n│   ├── column.ts\n│   ├── table.ts\n│   ├── function/                   ← standard SQL functions (count, sum, coalesce, ...)\n│   ├── datatypes/                  ← standard SQL types (int, text, bool, ...)\n│   └── renderer.ts                 ← Renderer\u003c\"standard\"\u003e entrypoint\n├── standard.ts                     ← NEW top-level barrel\n├── postgres/                       (existing — slimmed down)\n│   ├── internal/\n│   │   ├── dialect.ts              ← postgresDialect: spreads ...standardDialect + overrides\n│   │   ├── renderer.ts             (~30 lines wrapping shared renderer)\n│   │   └── dsl.ts                  (reduced to postgres-only extras)\n│   ├── query.ts                    ← re-exports Std.Query.* + adds postgres-only pieces\n│   └── ...                         (postgres-only extras stay: cast, type, schema, metadata)\n├── postgres.ts                     (re-export pattern unchanged at this level)\n├── mysql/  mysql.ts                (mirror of postgres slimming)\n└── sqlite/ sqlite.ts               (mirror)\n```\n\n## Mechanics\n\n### 1. Expand `SqlDialect` with renderer hooks\n\nToday's `SqlDialect` has four methods; the shared renderer needs more. Add methods for every spot the current per-dialect renderers branch on `dialect.name`:\n\n```ts\nexport interface SqlDialect\u003cName extends string = string\u003e {\n  readonly name: Name\n  // existing\n  quoteIdentifier(value: string): string\n  renderLiteral(value: unknown, state: RenderState, ctx?: RenderValueContext): string\n  renderTableReference(tableName: string, baseTableName: string, schemaName?: string): string\n  renderConcat(values: readonly string[]): string\n\n  // new — DDL\n  renderDdlString(value: string): string\n  renderDdlBytes(value: Uint8Array): string\n  renderDdlLiteral(value: unknown, state: RenderState, ctx?: RenderValueContext): string\n  renderCreateIndex(ddl, source, state, base): string\n  renderDropIndex(ddl, source, state, base): string\n\n  // new — mutations\n  renderInsertConflict(clause, state, base): string  // ON CONFLICT vs ON DUPLICATE KEY vs n/a\n  renderReturning(returning, state, base): string    // postgres/sqlite emit; mysql throws\n  renderUpdateOrderLimit(plan, state, base): string  // mysql allows; pg/sqlite reject\n\n  // new — type system\n  renderCast(targetDbType, expression, state, base): string  // dialect-specific type names\n  renderBooleanLiteral(value: boolean): string               // \"true\"/\"false\" vs \"1\"/\"0\"\n\n  // new — JSON\n  renderJsonExtract(target, path, state, base): string\n  renderJsonContains(target, value, state, base): string\n  renderJsonPathLiteral(segments): string\n  renderJsonMutation(kind, target, path, value, state, base): string\n}\n```\n\n`base` is the shared `renderExpression`/`renderQueryAst` so each dialect method can recurse for sub-expressions.\n\n### 2. Define `standardDialect` as the base\n\nIn `src/standard/dialect.ts`:\n\n```ts\nexport const standardDialect: SqlDialect\u003c\"standard\"\u003e = {\n  name: \"standard\",\n  quoteIdentifier: (v) =\u003e `\"${v.replaceAll('\"', '\"\"')}\"`,\n  renderLiteral: /* standard `?` params */,\n  renderTableReference: /* shared logic (already identical across all 3) */,\n  renderConcat: (v) =\u003e `(${v.join(\" || \")})`,\n  renderDdlString: (v) =\u003e `'${v.replaceAll(\"'\", \"''\")}'`,\n  renderBooleanLiteral: (v) =\u003e (v ? \"true\" : \"false\"),\n  renderInsertConflict: () =\u003e { throw new Error(\"ON CONFLICT not supported in standard SQL\") },\n  renderReturning: () =\u003e { throw new Error(\"RETURNING not supported in standard SQL\") },\n  renderCast: /* SQL:1992-ish CAST rendering */,\n  // ...sensible portable defaults; throw on dialect-only features\n}\n```\n\nConcrete dialects build themselves by spreading + overriding (e.g. `src/postgres/internal/dialect.ts`):\n\n```ts\nexport const postgresDialect: SqlDialect\u003c\"postgres\"\u003e = {\n  ...standardDialect,\n  name: \"postgres\",\n  renderLiteral: /* $N params */,\n  renderInsertConflict: /* ON CONFLICT (...) DO UPDATE */,\n  renderReturning: /* RETURNING clause */,\n  renderDropIndex: /* schema-qualified */,\n  renderJsonExtract: /* postgres -\u003e / -\u003e\u003e operators */,\n  // ...only what differs\n}\n```\n\nRecord-spread on `standardDialect` minimizes per-dialect code and makes the diff against the standard explicit.\n\n### 3. Consolidate the renderer\n\nCreate `src/internal/sql-expression-renderer.ts` (new shared module):\n\n1. Start from the postgres copy (median size).\n2. Replace each `if (dialect.name === \"X\")` branch with a call to a new `SqlDialect` method.\n3. Replace inline DDL/booleans/JSON/conflict/returning logic with `dialect.renderXxx(...)`.\n4. Delete the three per-dialect copies.\n\n`src/{mysql,postgres,sqlite}/internal/renderer.ts` shrinks to ~30-line wrappers that call the shared `renderQueryAst` with the dialect's record. Public `src/{mysql,postgres,sqlite}/renderer.ts` (24 lines each) doesn't change.\n\n### 4. Add the `MergeDialect` type rule\n\nIn `src/internal/query.ts`:\n\n```ts\ntype ConcreteDialect\u003cD\u003e = D extends \"standard\" ? never : D\ntype DialectConflictError\u003cA extends string, B extends string\u003e = {\n  readonly _tag: \"DialectConflict\"\n  readonly left: A\n  readonly right: B\n}\n\nexport type MergeDialect\u003cA extends string, B extends string\u003e =\n  [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [never, never] ? \"standard\"\n  : [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [never, infer C extends string] ? C\n  : [ConcreteDialect\u003cA\u003e, ConcreteDialect\u003cB\u003e] extends [infer C extends string, never] ? C\n  : ConcreteDialect\u003cA\u003e extends ConcreteDialect\u003cB\u003e\n    ? ConcreteDialect\u003cB\u003e extends ConcreteDialect\u003cA\u003e\n      ? A\n      : DialectConflictError\u003cA, B\u003e\n    : DialectConflictError\u003cA, B\u003e\n```\n\nUpdate existing `TupleDialect`, `ExtractDialect`, `SourceDialectOf`, and `PlanDialectOf` to use `MergeDialect` when combining. Update `DialectCompatiblePlan` so `\"standard\"` is universally accepted.\n\n### 5. Standard DSL namespace\n\n`src/standard/query.ts` mirrors `src/postgres/query.ts` but produces plans tagged `\"standard\"`. It re-exports from a new shared `src/internal/standard-dsl.ts` (extracted from the common subset of the three `internal/dsl.ts` files).\n\nConcrete dialects' `query.ts` files become:\n\n```ts\n// src/postgres/query.ts\nexport * from \"../standard/query.js\"\nexport { from as updateFrom } from \"./internal/dsl.js\"\nexport { returning } from \"./internal/dsl.js\"\n// ...\n```\n\nRe-exports needing re-typing (e.g. `update` needs to be retagged `\"postgres\"`) are thin wrappers; the rest are bare re-exports.\n\n## Migration sequence\n\nEach phase is a working, shippable commit. Public API (`mysql.ts`, `postgres.ts`, `sqlite.ts` surfaces) stays stable throughout — `standard.ts` is new, dialect modules gain exports, nothing is removed until the very end.\n\n### Phase 1 — Renderer consolidation (no API change)\n\n1. Expand `SqlDialect` interface in `src/internal/dialect.ts` with the new hooks.\n2. Implement the new hooks on `mysqlDialect`, `postgresDialect`, `sqliteDialect` (copy from the corresponding renderer files).\n3. Create `src/internal/sql-expression-renderer.ts` as the unified renderer, calling dialect hooks instead of branching on `dialect.name`.\n4. Rewrite `src/{mysql,postgres,sqlite}/internal/renderer.ts` as thin wrappers.\n5. Delete the three per-dialect `sql-expression-renderer.ts` files.\n6. Run full test suite — should pass with zero behavior change.\n\n### Phase 2 — Introduce `\"standard\"` as a dialect tag\n\n1. Add `MergeDialect` in `src/internal/query.ts`; update `TupleDialect`, `ExtractDialect`, `SourceDialectOf` to use it.\n2. Update `DialectCompatiblePlan` so `\"standard\"` is renderable by every engine.\n3. Add `standardDialect: SqlDialect\u003c\"standard\"\u003e` at `src/standard/dialect.ts`.\n4. Add `src/standard/renderer.ts` exposing a `Renderer\u003c\"standard\"\u003e`.\n5. Add tests confirming a hand-built `\"standard\"`-tagged plan renders identically on all three engines.\n\n### Phase 3 — Refactor dialect records to spread from `standardDialect`\n\n1. In each per-dialect `internal/dialect.ts`, replace literal record with `{ ...standardDialect, name: \"...\", /* overrides */ }`.\n2. Verify the three records are byte-identical to before, then remove redundant fields.\n\n### Phase 4 — Extract standard DSL\n\n1. Create `src/internal/standard-dsl.ts` by lifting the common subset of the three `internal/dsl.ts` files. Largest mechanical step (~6900-line files, expect ~5000 lines common).\n2. Add `src/standard/query.ts`, `src/standard/column.ts`, `src/standard/table.ts`, `src/standard/function/`, `src/standard/datatypes/` that wrap the shared DSL.\n3. Update each `src/{dialect}/internal/dsl.ts` to re-export from `internal/standard-dsl.ts` + extras.\n4. Update each `src/{dialect}/query.ts` to re-export `src/standard/query.ts` + specifics.\n5. Add `src/standard.ts` barrel mirroring `postgres.ts` (without the postgres-only `Cast`, `Type`, `Metadata`, `schema`, `enum`, `sequence`).\n\n### Phase 5 — Tighten\n\n1. Move datatypes that are universal SQL into `src/standard/datatypes/`; per-dialect folders keep only true natives (postgres ranges, mysql `set`).\n2. Update `package.json` exports to expose `./standard`.\n3. Add docs.\n\n## Critical files\n\n### Modified\n\n- `packages/querybuilder/src/internal/dialect.ts` — expand `SqlDialect` interface.\n- `packages/querybuilder/src/internal/query.ts` — add `MergeDialect`, update existing dialect type helpers.\n- `packages/querybuilder/src/internal/query.d.ts` — update `DialectCompatiblePlan`.\n- `packages/querybuilder/src/{mysql,postgres,sqlite}/internal/dialect.ts` — switch to `{ ...standardDialect, ... }`.\n- `packages/querybuilder/src/{mysql,postgres,sqlite}/internal/renderer.ts` — thin wrapper.\n- `packages/querybuilder/src/{mysql,postgres,sqlite}/query.ts` — re-export from standard + extras.\n- `packages/querybuilder/src/{mysql,postgres,sqlite}/internal/dsl.ts` — re-export from standard DSL + extras.\n\n### Created\n\n- `packages/querybuilder/src/internal/sql-expression-renderer.ts` (replaces 3× ~1820-line copies).\n- `packages/querybuilder/src/internal/standard-dsl.ts` (extracted from 3× ~6900-line copies).\n- `packages/querybuilder/src/standard/` folder: `dialect.ts`, `query.ts`, `column.ts`, `table.ts`, `renderer.ts`, `function/`, `datatypes/`.\n- `packages/querybuilder/src/standard.ts` barrel.\n\n### Deleted\n\n- `packages/querybuilder/src/{mysql,postgres,sqlite}/internal/sql-expression-renderer.ts`.\n\n## Reused infrastructure\n\n- `SqlDialect\u003cName\u003e` (`src/internal/dialect.ts:37`) — extended, not replaced.\n- `Renderer\u003cDialect\u003e` factory (`src/internal/renderer.ts:86`) — already engine-agnostic.\n- `DialectCompatiblePlan` / `DialectCompatibilityError` (`src/internal/query.d.ts:496,662`) — already the compatibility gate.\n- `TupleDialect`, `ExtractDialect`, `PlanDialectOf`, `SourceDialectOf` (`src/internal/query.ts:144,468,1466`; `src/internal/query.d.ts:496`) — already track dialect across composition; only their merge rule changes.\n- `internal/query-ast.ts`, `internal/expression-ast.ts`, `internal/json/`, `internal/predicate/`, `internal/coercion/`, `internal/runtime/` — already dialect-neutral; untouched.\n- `internal/column.ts`, `internal/table.ts` — already the shared base for dialect column/table wrappers.\n\n## Notes and trade-offs\n\n- The `MergeDialect` type is load-bearing. If wrong (especially around `\"standard\"` being a subtype), error messages will be confusing. Recommend writing dedicated type-level tests for it first thing in Phase 2.\n- Phase 4 (DSL extraction) is the largest blast radius. If painful, the refactor still has high value after Phases 1+2+3 — at that point we've eliminated ~5000 lines of renderer duplication and added a working `\"standard\"` dialect, even if the DSL stays per-dialect for now.\n- Postgres-only features (`Cast`, `Type`, `Metadata`, `schema`, `enum`, `sequence`) remain Postgres-only; additive and out of scope.\n- The integration test docker compose covers all three engines, giving strong end-to-end verification at every phase.\n","design":"# Design notes\n\n## Why this approach\n\nTwo abstraction axes are at play:\n\n1. **Renderer-layer hooks (mechanism).** Today, the three `*/internal/sql-expression-renderer.ts` files duplicate ~70–80% of their content and contain inline `dialect.name === \"...\"` switches. Adding hooks to `SqlDialect` for each branched spot lets one shared renderer cover all three engines. A `standardDialect` provides portable defaults; concrete dialects override only what differs.\n\n2. **DSL-layer dialect provenance (user surface).** Each DSL piece (`update`, `from`, `where`, ...) carries a dialect tag in its types. Importing a piece from `Pg.Query` narrows the plan's dialect tag to `\"postgres\"`. Mixing pieces from two concrete dialects is a compile error. This makes \"is this query portable?\" a first-class type-level question.\n\nThe two axes are complementary: the DSL-layer model is what users see; the renderer-layer model is what makes it implementable.\n\n## Why type-only specialization (vs. AST-carried render closures)\n\nConsidered but rejected: have `Pg.Query.from(...)` produce an AST node carrying its own dialect-specific render closure, so the shared renderer doesn't need to know postgres-specific syntax.\n\nRejected because:\n- The current codebase already has a single shared AST (`src/internal/query-ast.ts`, `expression-ast.ts`) that all three dialects build. Type-only specialization fits the existing grain with the smallest change.\n- Coupling AST data to render logic hurts the AST's reusability for any future tooling (analyzer, optimizer, formatter).\n- With expanded `SqlDialect` hooks, the renderer can emit correct dialect-specific SQL from a universal AST without each AST node knowing a render strategy.\n\nSo pieces are typed factories that produce the same universal AST; the type tag is the only thing dialect-specific pieces add over standard ones.\n\n## Why `\"standard\"` is a bottom element (vs. plain union)\n\nA `\"standard\"` plan should be renderable by every engine. A `\"postgres\" | \"standard\"` plan is conceptually a `\"postgres\"` plan — the `\"standard\"` part doesn't constrain anything new. So `MergeDialect` collapses `\"standard\" ∪ X → X`, giving cleaner inferred types. This is what the user asked for (\"narrow to the most-specific dialect\").\n\n`DialectCompatiblePlan\u003cP, Engine\u003e` must accept any `P` whose dialect tag is `\"standard\"` or `Engine`. Anything else surfaces as a `DialectCompatibilityError` at the engine boundary.\n\n## Why expand `SqlDialect` (vs. visitor-pattern record per renderer)\n\nAlternative considered: define a `Renderer = { renderSelect, renderFrom, renderInsert, ... }` record, with a `standardRenderer` and per-dialect spreads.\n\nChose to expand `SqlDialect` instead because:\n- The existing renderer is already organized as one function (`renderQueryAst`) calling many internal helpers. Splitting every helper into a record field would require re-architecting the whole renderer.\n- `SqlDialect` already exists with 4 hooks; adding 12 more keeps the abstraction at one consistent layer.\n- The renderer can stay as one big function calling `dialect.renderXxx(...)` at the exact branch points. Minimal churn on the renderer skeleton.\n\nThe downside: `SqlDialect` becomes larger. Mitigated by the fact that `standardDialect` provides defaults so concrete dialects stay short.\n\n## Sequencing rationale\n\nPhase ordering is chosen so that **each phase is independently shippable** and the public API never breaks during the transition:\n\n1. **Phase 1 first** because it's the largest value (eliminates ~5000 lines of duplication) with no API change. Pure internal refactor.\n2. **Phase 2 next** because it's prerequisite for any user-facing `standard.*` imports. Type-system change only.\n3. **Phase 3** uses Phase 2's `standardDialect` to slim the concrete dialect records.\n4. **Phase 4** is the largest blast radius and depends on Phase 2. If painful, can stop after Phase 3 and still ship most of the value.\n5. **Phase 5** is polish.\n\n## Open risks\n\n- **`MergeDialect` correctness.** Type-level tests must be written first thing in Phase 2 before the rest of the type-system updates depend on it.\n- **Hidden divergence in the three renderers.** A golden-file test of ~50 representative queries (covering ON CONFLICT, UPDATE...FROM, JSON ops, CAST, RETURNING, CREATE/DROP INDEX) should be added *before* Phase 1 begins so we can verify byte-identical SQL after consolidation.\n- **DSL extraction scope.** The three `internal/dsl.ts` files are ~6900 lines each. Lifting the common subset will be the most labor-intensive step. Acceptable to defer to a follow-up if Phase 4 turns out larger than expected.\n","acceptance_criteria":"1. Per-dialect `*/internal/sql-expression-renderer.ts` files are deleted; one shared `src/internal/sql-expression-renderer.ts` produces byte-identical SQL for all three engines (verified by golden-file test of ~50 representative queries covering ON CONFLICT, UPDATE...FROM, JSON ops, CAST, RETURNING, CREATE/DROP INDEX). 2. `standardDialect: SqlDialect\u003c\"standard\"\u003e` exists at `src/standard/dialect.ts`; `mysqlDialect`/`postgresDialect`/`sqliteDialect` are defined as `{ ...standardDialect, ... overrides }`. 3. `MergeDialect\u003cA, B\u003e` is defined in `src/internal/query.ts` and used by `TupleDialect`, `ExtractDialect`, `SourceDialectOf`, `PlanDialectOf`. Type-level tests verify: `standard ∪ standard = standard`; `standard ∪ X = X`; `X ∪ X = X`; `postgres ∪ mysql = DialectConflictError`. 4. `DialectCompatiblePlan` accepts `standard`-tagged plans for every engine. 5. Public `src/standard.ts` namespace exports `Query`, `Column`, `Table`, `Datatypes`, `Function`, `Renderer` that produce `standard`-tagged plans. 6. Mixing one `Pg.Query` piece with `Std.Query` pieces produces a plan typed `postgres`, renderable by the postgres renderer but a compile error against `mysql.Renderer`/`sqlite.Renderer`. 7. Existing public API (`mysql.ts`, `postgres.ts`, `sqlite.ts` surfaces) unchanged; existing docs/README examples compile and run unchanged. 8. Unit tests, type tests (`bunx tsgo -p tsconfig.type-tests.json`), and integration tests (`docker-compose.integration.yml`) all pass on all three engines. 9. `package.json` exports include `./standard`.","notes":"Plan file: /Users/ramazan/.claude/plans/the-current-package-implements-expressive-cocoa.md. Approved by user on 2026-05-19.","status":"open","priority":1,"issue_type":"epic","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:50:55Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-19T20:50:55Z","labels":["architecture","dialect","querybuilder","refactor"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.4","title":"P1 validate effect-db rows with SqlSchema","description":"Use v4 effect/unstable/sql/SqlSchema for high-risk effect-db result rows, starting with Postgres introspection and migration ledger reads.","acceptance_criteria":"Introspection and migration ledger row shapes are represented by internal schemas; selected unsafe typed row reads are replaced or wrapped with SqlSchema helpers; checksum/migration behavior is unchanged except for earlier shape failures; behavior/integration tests cover happy path and malformed-row failure where feasible.","notes":"Status: open. Validation plan: postgres-introspector behavior tests, postgres-migrate behavior tests, CLI integration tests if Postgres is available. Keep source-code discovery independent of SqlSchema.\nImplemented in second migration slice. Added v4 SqlSchema row decoders around Postgres catalog introspection reads and applied-migration ledger reads. Malformed catalog and ledger row tests now assert schema decode failure before metadata/migration state is built. Checks passed: bun --config ./bunfig.test.toml test test/internal/behavior/postgres-introspector.behavior.test.ts test/internal/behavior/postgres-migrate.behavior.test.ts; bunx tsgo -p tsconfig.type-tests.json; bun run test:types; bun run build; bun run test; bun run test:pack.","status":"closed","priority":1,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:38Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:45:33Z","started_at":"2026-05-13T02:42:58Z","closed_at":"2026-05-13T02:45:33Z","close_reason":"effect-db catalog and migration ledger row reads now decode through v4 SqlSchema with malformed-row tests and repo gates passing.","labels":["effect-db","effect-v4","migration","sqlschema"],"dependencies":[{"issue_id":"effect-qb-dn3.4","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:38Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.4","depends_on_id":"effect-qb-dn3.3","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.3","title":"P1 adopt v4 SQL core","description":"Replace standalone @effect/sql imports with effect/unstable/sql imports and update built-in executor transaction/stream handling for v4 SqlClient semantics.","acceptance_criteria":"Executor imports use v4 SQL core; streamFromSqlClient no longer relies on stale global TransactionConnection behavior; transaction consumption semantics remain covered; UniqueViolation reason usage is documented or tested without deleting existing dialect-specific SQLSTATE catalogs.","notes":"Status: open. Validation plan: executor behavior tests, executor stream type tests, SQL error behavior tests, and integration tests if driver setup is available.\nImplemented in first migration slice. SQL imports now use effect/unstable/sql/*; v4 driver packages are version-aligned; streamFromSqlClient uses the client transactionService and v4 Stream APIs instead of stale TransactionConnection/mapChunks paths. Existing dialect-specific SQLSTATE catalogs were preserved; UniqueViolation v4 reason behavior remains documented in docs/effect-v4-opportunities.md. Verified by executor behavior tests, SQL error behavior tests, full tsgo type-test project, build, pack, and full Bun test script.","status":"closed","priority":1,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:33Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:41:55Z","closed_at":"2026-05-13T02:41:55Z","close_reason":"v4 SQL core imports and transaction/stream handling implemented; executor and SQL error behavior tests passed.","labels":["effect-v4","migration","sql"],"dependencies":[{"issue_id":"effect-qb-dn3.3","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:33Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.3","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"effect-qb-dn3.2","title":"P1 replace experimental VariantSchema","description":"Move table select/insert/update schema derivation from @effect/experimental/VariantSchema to effect/unstable/schema/VariantSchema while preserving deriveSchemas public behavior and generated declaration intent.","acceptance_criteria":"@effect/experimental is no longer needed by querybuilder; deriveSchemas still derives select, insert, and update schemas with generated/default/nullable/primary-key/brand semantics; table behavior and type tests pass; generated declarations are intentionally updated if v4 types require it.","notes":"Status: open. Depends on the mechanical Schema API migration compiling. Validation plan: table behavior tests, table type tests, README schema examples, and tsgo type-test project.\nImplemented in first migration slice by moving table schema derivation to effect/unstable/schema/VariantSchema and removing @effect/experimental from package manifests. deriveSchemas now returns v4 Schema.Decoder values for select/insert/update. Verified by table behavior, generated README type tests, packed consumer test, and full tsgo type-test project.","status":"closed","priority":1,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:25Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:41:43Z","closed_at":"2026-05-13T02:41:43Z","close_reason":"VariantSchema migration implemented with @effect/experimental removed and deriveSchemas preserved under v4 tests.","labels":["effect-v4","migration","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.2","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:25Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.2","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"effect-qb-yll.4","title":"Phase 4 — Extract standard DSL into src/internal/standard-dsl.ts and src/standard/* namespace","description":"# Phase 4 — Extract standard DSL\n\nDepends on Phase 2 (the `\"standard\"` tag must exist for DSL pieces to carry).\n\nLargest blast radius of the refactor. The three `internal/dsl.ts` files (~6900 lines each — mysql 6853, postgres 6868, sqlite 6927) hold the DSL surface for `select`, `from`, `where`, `update`, `insert`, `delete`, `join`, `groupBy`, etc. They share most of their content; extracting the common subset into a shared module + a `standard/` public namespace is the goal.\n\n## Substeps\n\n1. **Map common vs divergent.** Run a structured diff over the three `internal/dsl.ts` files to enumerate which exported functions are byte-identical, which differ only in their phantom dialect tag, and which are dialect-specific (postgres-only `from` on UPDATE, postgres/sqlite `returning`, postgres array operators, mysql-specific `LOW_PRIORITY`, etc.).\n2. **Create `src/internal/standard-dsl.ts`** by lifting the common subset — expect ~5000 lines extracted from each. Internally the functions produce AST nodes tagged `\"standard\"`. Re-typed wrappers (Phase 3 product) handle dialect retagging.\n3. **Create `src/standard/query.ts`, `src/standard/column.ts`, `src/standard/table.ts`** as thin barrels over `src/internal/standard-dsl.ts` producing `\"standard\"`-tagged plans/columns/tables. Mirror the structure of `src/postgres/query.ts` etc.\n4. **Create `src/standard/function/`** (`index.ts`, `core.ts`, `aggregate.ts`, `string.ts`, `temporal.ts`, `window.ts`) extracted from the common subset of the three per-dialect `function/` folders. Dialect-specific functions stay in their own folders (e.g. postgres `nextVal`, `uuid_generate_v4`).\n5. **Create `src/standard/datatypes/`** containing the truly portable SQL types (`int`, `bigint`, `text`, `varchar`, `bool`, `date`, `timestamp`, etc.). Dialect-specific types (postgres ranges, mysql `set`/`enum`, sqlite-only types) stay in per-dialect `datatypes/`.\n6. **Update each `src/{mysql,postgres,sqlite}/internal/dsl.ts`** to re-export the standard DSL from `src/internal/standard-dsl.ts` and add only the dialect-specific extras. For functions that need re-typing to the concrete dialect (e.g. `update` returning `UpdatePlan\u003c\"postgres\"\u003e` instead of `UpdatePlan\u003c\"standard\"\u003e`), provide a thin wrapper.\n7. **Update each `src/{mysql,postgres,sqlite}/query.ts`** to `export * from \"../standard/query.js\"` and re-export only dialect-specific pieces from `./internal/dsl.js`.\n8. **Create `src/standard.ts`** barrel mirroring `src/postgres.ts` (line by line) but without the postgres-only `Cast`, `Type`, `Metadata`, `schema`, `enum`, `sequence` exports.\n9. **Cross-engine portable-plan tests.** Build queries entirely from `Std.Query.*` and assert each renders successfully through every engine's renderer. Build a mixed query (`Std.Query.update(...).set(...).from(Pg.Query.from(...))`) and assert the resulting plan's `PlanDialectOf` is exactly `\"postgres\"`; passing it to `mysql.Renderer.make()` is a compile error.\n10. **Run** full test suite + integration tests on all three engines.\n\n## Files touched\n\n**Modified:** `src/{mysql,postgres,sqlite}/internal/dsl.ts`; `src/{mysql,postgres,sqlite}/query.ts`; `src/{mysql,postgres,sqlite}/column.ts`; `src/{mysql,postgres,sqlite}/table.ts`.\n\n**Created:** `src/internal/standard-dsl.ts`; `src/standard/query.ts`, `src/standard/column.ts`, `src/standard/table.ts`, `src/standard/function/{index,core,aggregate,string,temporal,window}.ts`, `src/standard/datatypes/{index,spec}.ts`; `src/standard.ts`.\n\n## Acceptance\n\n- `src/standard.ts` is importable and exports `Query`, `Column`, `Table`, `Function`, `Datatypes`, `Renderer` (and any other portable namespaces).\n- Per-dialect `internal/dsl.ts` files re-export from `src/internal/standard-dsl.ts` and contain only dialect-specific extras.\n- A query built entirely from `Std.Query.*` produces a plan typed `\"standard\"` that renders byte-identically on all three engines.\n- A query mixing `Std.Query.update(...)` with `Pg.Query.from(...)` types as `PlanDialectOf = \"postgres\"`.\n- Mixing `Pg.Query` and `My.Query` pieces produces a `DialectConflictError` at the AST construction site.\n- Existing `mysql.Query`/`postgres.Query`/`sqlite.Query` imports work unchanged for existing users.\n- All unit/type/integration tests pass.\n\n## Notes\n\n- This phase is the largest scope. If it proves too expansive, **Phases 1–3 still deliver the bulk of the value** (eliminated renderer duplication and working standard dialect tag). Phase 4 can be split into 4a (extract standard DSL) and 4b (per-dialect re-export migration) if needed.\n- The dialect-specific pieces remaining after extraction inform the long-term per-dialect surface and help document which features are truly dialect-bound.\n","acceptance_criteria":"src/standard.ts barrel importable; Std.Query.* produces \"standard\"-tagged plans that render byte-identically on all three engines; mixing Std.Query.update(...) with Pg.Query.from(...) types as PlanDialectOf=\"postgres\" and is renderable only by postgres.Renderer (compile error on mysql.Renderer); mixing Pg.Query with My.Query surfaces DialectConflictError; existing mysql.Query/postgres.Query/sqlite.Query imports unchanged for current users; unit/type/integration suites pass.","notes":"Partial public namespace exists: src/standard.ts exports Query, Column, Table, Datatypes, Function, Renderer, and package exports expose ./standard. This is not the requested extraction: src/internal/standard-dsl.ts was not created, per-dialect internal/dsl.ts and query.ts were not converted to re-export a standard DSL, and the standard Query surface currently uses a thin runtime retag wrapper over the existing postgres DSL to avoid duplicating the 7k-line DSL and blowing up tsgo.\n2026-05-19 update: src/internal/standard-dsl.ts now exists and Standard.Query re-exports from it instead of runtime-retagging the postgres DSL. Standard temporal functions now construct standard-tagged expressions directly. Gates passed: bun test, bun run test:types, bun run build, bun run test:pack, bun run test:integration, git diff --check. Still open: mysql/postgres/sqlite internal/dsl.ts files remain full concrete copies and have not been slimmed to re-export standard-dsl plus dialect-specific extras.\n2026-05-23 update: standard namespace behavior is covered for select, CTE, joins, grouping, ordering, pagination, insert/update/delete, and DDL across Standard/Postgres/MySQL/SQLite renderers. Remaining phase-4 work is the broader per-dialect DSL slimming/re-export refactor, not a blocker for standard namespace usage.\n2026-05-24 update: Root package import now aliases the standard portable namespace as Sql: effect-qb exports the Sql namespace and direct standard Query/Table/Column/Function surface, with type tests, README generated tests, build, behavior, and packed-consumer smoke passing.","status":"open","priority":2,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:53:16Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T03:08:21Z","labels":["architecture","dialect","dsl","phase-4","querybuilder","refactor"],"dependencies":[{"issue_id":"effect-qb-yll.4","depends_on_id":"effect-qb-yll","type":"parent-child","created_at":"2026-05-19T13:53:16Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-yll.4","depends_on_id":"effect-qb-yll.2","type":"blocks","created_at":"2026-05-19T13:53:30Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"effect-qb-yll.3","title":"Phase 3 — Refactor per-dialect dialect records to spread from standardDialect","description":"# Phase 3 — Refactor dialect records to spread from `standardDialect`\n\nDepends on Phase 2 (`standardDialect` must exist).\n\nOnce `standardDialect` provides defaults for every `SqlDialect` hook, concrete dialect records can stop reimplementing fields that match the standard. This makes the per-dialect diff explicit and tiny.\n\n## Substeps\n\n1. In `src/postgres/internal/dialect.ts`, replace the record literal with:\n    ```ts\n    import { standardDialect } from \"../../standard/dialect.js\"\n    export const postgresDialect: SqlDialect\u003c\"postgres\"\u003e = {\n      ...standardDialect,\n      name: \"postgres\",\n      renderLiteral: /* $N placeholders */,\n      renderInsertConflict: /* ON CONFLICT ... DO UPDATE */,\n      renderReturning: /* RETURNING ... */,\n      renderDropIndex: /* schema-qualified */,\n      renderJsonExtract: /* -\u003e / -\u003e\u003e operators */,\n      renderJsonContains: /* @\u003e */,\n      renderJsonMutation: /* jsonb_set / jsonb_insert */,\n      renderUpdateOrderLimit: () =\u003e { throw new Error(\"postgres update does not support order/limit\") },\n      // ...keep only fields that differ from standardDialect\n    }\n    ```\n2. Repeat for `src/mysql/internal/dialect.ts` (overrides: backtick `quoteIdentifier`, `?` placeholder, `concat()` function, `ON DUPLICATE KEY UPDATE`, `x'...'` DDL bytes, mysql-specific CAST type names like `char(36)`/`datetime`, mysql JSON `$.path` literal, MySQL UPDATE `ORDER BY/LIMIT`, mysql throws on `renderReturning`).\n3. Repeat for `src/sqlite/internal/dialect.ts` (overrides: `1`/`0` boolean literals, `text`/`time`/`datetime` CAST type names, sqlite JSON path syntax, sqlite throws on `renderInsertConflict`'s postgres-only options if any).\n4. Verify each dialect record produces the exact same `SqlDialect` behavior as before — diff the resolved record (`{ ...standardDialect, ... }`) field-by-field against the pre-Phase-3 literal record in a snapshot test.\n\n## Files touched\n\n**Modified:** `src/{mysql,postgres,sqlite}/internal/dialect.ts`.\n\n**Created:** dialect-record snapshot test (optional, can be deleted after verification).\n\n## Acceptance\n\n- All three per-dialect `internal/dialect.ts` files use `{ ...standardDialect, name, ...overrides }`.\n- Per-dialect files contain only fields that genuinely differ from the standard.\n- Golden SQL snapshot tests from Phase 1 still produce byte-identical output.\n- Full test suite passes.\n","acceptance_criteria":"All three per-dialect internal/dialect.ts files use { ...standardDialect, name, ...overrides }; only fields that genuinely differ from the standard are present; golden SQL snapshot from Phase 1 still byte-identical; full test suite passes.","notes":"2026-05-23 update: built-in dialect records currently spread standardDialect and override name, literal/identifier/table rendering, concat where needed, and renderer functions. Dependency ordering still prevents closing while phase-1/phase-2 remain open in Beads.","status":"open","priority":2,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:53:09Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T01:48:44Z","labels":["architecture","dialect","phase-3","querybuilder","refactor"],"dependencies":[{"issue_id":"effect-qb-yll.3","depends_on_id":"effect-qb-yll","type":"parent-child","created_at":"2026-05-19T13:53:08Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-yll.3","depends_on_id":"effect-qb-yll.2","type":"blocks","created_at":"2026-05-19T13:53:29Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.9","title":"P2 evaluate schema Model APIs after VariantSchema migration","description":"Evaluate effect/unstable/schema/Model after the safe VariantSchema migration is complete, specifically whether Model select/insert/update/json helpers can support a future model-first table API.","acceptance_criteria":"Decision is recorded with code examples or a prototype; no existing table DSL behavior changes unless a separate implementation bead is created; Model helpers Generated/Sensitive/FieldOption are assessed against effect-qb column metadata.","notes":"Status: open. This is explicitly deferred until deriveSchemas is stable on v4 VariantSchema; do not block the mechanical migration on a broader model-first redesign.","status":"open","priority":2,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:19:32Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:19:32Z","labels":["effect-v4","migration","model","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.9","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:19:32Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.9","depends_on_id":"effect-qb-dn3.2","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.7","title":"P2 harden JSON path schema inference for v4","description":"Revisit packages/querybuilder/src/internal/runtime/schema.ts manual Schema AST inspection under v4, adding focused coverage for JSON path narrowing before changing the inference logic.","acceptance_criteria":"Focused tests cover object, tuple, union, optional/missing, transformed/refined, wildcard/non-exact fallback, and JSON-compatible fallback cases; v4 AST assumptions are documented in code or bead notes; public helpers are preferred where available without weakening SQL semantics.","notes":"Implemented. Exact JSON path schema inference now walks the original v4 Schema AST rather than SchemaAST.toType so encoded-side field transformations such as NumberFromString are preserved for JSON subvalues. Added focused runtime-schema coverage for object paths, tuple indexes, unions, optional properties, missing-path fallback, wildcard fallback, refined leaves, transformations, and json/toJson compatible-vs-non-compatible fallback. Checks passed: bun --config ./bunfig.test.toml test test/internal/behavior/runtime-schema.behavior.test.ts test/public/behavior/json.behavior.test.ts; bunx tsgo -p tsconfig.type-tests.json; bun run test:types; bun run build; bun run test; bun run test:pack.","status":"in_progress","priority":2,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:19:08Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:56:33Z","started_at":"2026-05-13T02:52:39Z","labels":["effect-v4","json","migration","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.7","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:19:08Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.7","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.6","title":"P2 preserve v4 Schema issues in RowDecodeError","description":"Improve row decoding to use v4 Result/issue-shaped Schema APIs instead of exception-only decode flow, preserving structured SchemaIssue detail for users.","acceptance_criteria":"RowDecodeError exposes or formats structured schema issue detail; existing normalization/schema phase behavior remains compatible; JSON nested schema failures produce stable path/message output; executor behavior tests cover decode failure details.","notes":"Implemented. RowDecodeError now preserves v4 SchemaError.message and SchemaError.issue on schema decode failures while keeping the existing cause field. decodeProjectionValue uses Schema.decodeUnknownExit and extracts SchemaError from Exit instead of relying on decodeUnknownSync exceptions. Added nested JSON decode failure coverage asserting stable projection metadata plus schemaError detail. Checks passed: bun --config ./bunfig.test.toml test test/public/behavior/executor.behavior.test.ts; bunx tsgo -p tsconfig.type-tests.json; bun run test:types; bun run build; bun run test; bun run test:pack.","status":"closed","priority":2,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:59Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:52:24Z","started_at":"2026-05-13T02:49:58Z","closed_at":"2026-05-13T02:52:24Z","close_reason":"RowDecodeError now carries structured v4 Schema issue details and nested JSON decode failure coverage passes with repo gates.","labels":["effect-v4","errors","executor","migration","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.6","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:59Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.6","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.5","title":"P2 add opt-in v4 Schema codecs","description":"Expose or document opt-in column/runtime helpers for v4 Schema codecs such as DateFromString, BigIntFromString, BigDecimalFromString, DurationFromString, and encoded string codecs without silently changing default column decoded types.","acceptance_criteria":"Default temporal/numeric/binary decoded types remain stable; opt-in recipes or helpers are available for supported codecs; invalid schema overrides still fail at type level; README/generated type snippets are updated and checked.","notes":"Status: open. Validation plan: column schema compatibility type tests, README generated type tests, runtime decode tests for at least one temporal/numeric codec.\nImplemented in third migration slice. Documented opt-in v4 codec pairings through C.schema for BigIntFromString, BigDecimalFromString, DurationFromString, and flipped Uint8ArrayFromBase64. Added type tests proving opt-in decoded types and default column types remain unchanged, plus executor behavior coverage for codec transforms after canonical normalization. Checks passed: bun --config ./bunfig.test.toml test test/public/behavior/executor.behavior.test.ts; bunx tsgo -p tsconfig.type-tests.json; bun run test:types; bun run build; bun run test; bun run test:pack.","status":"closed","priority":2,"issue_type":"task","assignee":"Ramazan Elsunkaev","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:18:50Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:48:39Z","started_at":"2026-05-13T02:45:55Z","closed_at":"2026-05-13T02:48:39Z","close_reason":"v4 codec usage is documented and covered by type/runtime tests while default column decoded types remain unchanged.","labels":["codecs","effect-v4","migration","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.5","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:18:50Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.5","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-yll.5","title":"Phase 5 — Consolidate portable datatypes, expose ./standard in package.json, docs","description":"# Phase 5 — Tighten\n\nDepends on Phase 4 (`src/standard/` must exist).\n\nPolish phase: move portable datatypes into `standard/`, expose the public entry point in `package.json`, and add documentation.\n\n## Substeps\n\n1. **Datatypes audit.** Walk through `src/{mysql,postgres,sqlite}/datatypes/`. Move every type that's universal SQL into `src/standard/datatypes/`. Per-dialect folders should retain only true natives: postgres ranges/`citext`/`jsonb`/array types; mysql `set`/`enum`/`mediumtext`; sqlite-specific affinity quirks. Update per-dialect `datatypes/index.ts` to re-export from `standard/datatypes/` and add the extras.\n2. **`package.json` exports.** Add `\"./standard\"` to the `exports` map in `packages/querybuilder/package.json`, mirroring the existing `\"./mysql\"`, `\"./postgres\"`, `\"./sqlite\"` shape. Verify `dist/standard/` is produced by the build.\n3. **Documentation.**\n   - Add a `docs/` page explaining the new `standard` namespace, the dialect tag rule (`\"standard\"` is portable; mixing concrete dialects is a compile error), and migration tips for users who want portable queries.\n   - Update `README.md` with a short \"Writing portable queries\" section using the standard namespace.\n   - Add an example file under `docs/examples/` demonstrating a portable query and a postgres-narrowed query.\n4. **Build + smoke test.** Run `bun run build` per package. Verify `dist/standard/` shape mirrors `dist/postgres/` for the equivalent files. Run the README examples unchanged.\n\n## Files touched\n\n**Modified:** `packages/querybuilder/package.json`; `src/{mysql,postgres,sqlite}/datatypes/index.ts`; `README.md`; `docs/*`.\n\n**Created:** docs page for standard namespace; example file(s).\n\n**Possibly moved:** specific datatype definitions from per-dialect to `src/standard/datatypes/`.\n\n## Acceptance\n\n- `package.json` exposes `./standard` as a public entry point; `dist/standard/` builds successfully.\n- Per-dialect `datatypes/` folders contain only genuinely dialect-specific types.\n- README and docs reflect the new namespace.\n- All tests pass.\n- `import * as Std from \"@effect-qb/querybuilder/standard\"` works in a fresh project.\n","acceptance_criteria":"package.json exposes ./standard; dist/standard/ builds; per-dialect datatypes/ folders contain only genuinely dialect-specific types; README and docs include a 'Writing portable queries' section using the standard namespace; an example file demonstrates a portable query and a postgres-narrowed query; all tests pass.","notes":"Partial docs/build pieces are present: package exports include ./standard, packages/querybuilder/scripts/build.ts emits standard.js, docs/standard-namespace.md and docs/examples/standard-query.ts were added, and README/package README mention portable queries. Not acceptance-complete: portable datatypes were not audited/moved out of per-dialect folders, and docs are minimal.\n2026-05-23 update: standard column coverage now includes varchar, char, bigint, real, and blob with runtime type coverage; package build and pack smoke pass for effect-qb/standard. Remaining phase-5 work is any deeper per-dialect datatype folder audit/documentation expansion.","status":"open","priority":3,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-19T20:53:23Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-24T01:48:19Z","labels":["architecture","dialect","docs","phase-5","querybuilder","refactor"],"dependencies":[{"issue_id":"effect-qb-yll.5","depends_on_id":"effect-qb-yll","type":"parent-child","created_at":"2026-05-19T13:53:22Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-yll.5","depends_on_id":"effect-qb-yll.4","type":"blocks","created_at":"2026-05-19T13:53:30Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.10","title":"P3 decide schema optics follow-up","description":"Track the schema optics/lenses follow-up from docs/effect-v4-schema-optics.md so related v4 work remains under the epic.","acceptance_criteria":"Decision remains linked to the epic; Effect Optic is documented only for decoded value helpers unless a separate JsonPath.focus implementation bead is opened; no mechanical migration work depends on optics.","notes":"Status: deferred by current evaluation. docs/effect-v4-schema-optics.md verdict: useful narrowly for decoded values and API inspiration, not a replacement for schema derivation/runtime inference/SQL JSON paths.","status":"closed","priority":3,"issue_type":"decision","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:19:44Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:42:11Z","closed_at":"2026-05-13T02:42:11Z","close_reason":"Optics follow-up decision recorded in docs/effect-v4-schema-optics.md; no mechanical v4 migration work depends on optics.","labels":["effect-v4","migration","optics","schema"],"dependencies":[{"issue_id":"effect-qb-dn3.10","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:19:44Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"effect-qb-dn3.8","title":"P3 migrate effect-db CLI/platform imports","description":"Move effect-db CLI/platform usage to v4-aligned packages/imports and evaluate v4 CLI improvements for destructive confirmation flows without breaking non-interactive CI usage.","acceptance_criteria":"CLI imports are v4-aligned; existing commands still parse and run; boolean flag behavior is verified, especially destructive flags; any interactive confirmation is additive and bypassable for CI.","notes":"Status: open. Validation plan: postgres CLI behavior/integration tests and direct --help/parse smoke tests with Bun.\nImplemented in first migration slice. effect-db CLI now imports Command/Flag from effect/unstable/cli and BunRuntime/BunServices from @effect/platform-bun v4. Verified root and subcommand help paths: bun packages/database/src/cli.ts --help; bun packages/database/src/cli.ts push --help; bun packages/database/src/cli.ts migrate generate --help; bun packages/database/src/cli.ts migrate down --help. Boolean flags remain visible for non-interactive CI flows.","status":"closed","priority":3,"issue_type":"task","owner":"relsunkaev@outlook.com","created_at":"2026-05-13T02:19:21Z","created_by":"Ramazan Elsunkaev","updated_at":"2026-05-13T02:42:05Z","closed_at":"2026-05-13T02:42:05Z","close_reason":"effect-db CLI/platform imports migrated to v4 and help/flag parsing smoke tests passed.","labels":["cli","effect-db","effect-v4","migration"],"dependencies":[{"issue_id":"effect-qb-dn3.8","depends_on_id":"effect-qb-dn3","type":"parent-child","created_at":"2026-05-12T19:19:20Z","created_by":"Ramazan Elsunkaev","metadata":"{}"},{"issue_id":"effect-qb-dn3.8","depends_on_id":"effect-qb-dn3.1","type":"blocks","created_at":"2026-05-12T19:20:09Z","created_by":"Ramazan Elsunkaev","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
